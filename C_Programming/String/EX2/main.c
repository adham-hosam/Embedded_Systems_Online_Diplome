#include <stdio.h>
//C program to find the length of a string
//Adham Hosam

int main() {
    char str[100];
    int i;
    printf("Enter a string:");
    gets(str);
    for ( i = 0; str[i] !=0 ; ++i) {
    }
    printf("Length of string = %d",i);


    return 0;
}
